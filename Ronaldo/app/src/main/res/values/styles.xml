<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>                <!-- 主色调 -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>        <!-- 主色调的暗色-->
        <item name="colorAccent">@color/colorAccent</item>                  <!-- 强调的色调 -->
        <!--<item name="android:windowNoTitle">true</item>-->
        <!--buildToolsVersion大于21，不可以使用windowActionBar-->
        <!--<item name="android:windowActionBar">false</item> &lt;!&ndash; actionbar隐藏 &ndash;&gt;-->
        <item name="android:windowBackground">@color/window_background</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@android:color/transparent</item>
    </style>
    <style name="AppTheme" parent="AppTheme.Base">
    </style>
</resources>
